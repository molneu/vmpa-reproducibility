# Figure Scripts

Each subfolder contains the scripts used to reproduce one manuscript figure.

Expected layout after downloading Figshare data:

```text
reproducibility/
  figure1/
    data/
    results/
    scripts/
  figure2/
    data/
    results/
    scripts/
  ...
```

The `data/` and `results/` folders are ignored by Git.
